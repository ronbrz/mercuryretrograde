* Retrograde Mercury site

Simple server written using the scotty haskell web framework.
It checks whether or not Mercury is in retrograde.

** Run
Uses stack for dependencies, run server with command

``` sh
$ stack run
```
